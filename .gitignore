# Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Terraform plan files
*.tfplan

# .terraform directory (plugins, modules, etc.)
.terraform/

# Lock file (optional—keep it if you want to lock provider versions)
.terraform.lock.hcl